# Selection Examples

This directory contains tiny illustrative examples of Script B outputs.

These files are:
- schema examples only,
- intentionally truncated,
- non-authoritative,
- and not provenance-preserving runtime artifacts.

Real run-selection provenance is generated dynamically by Script B and should remain local to the execution environment.