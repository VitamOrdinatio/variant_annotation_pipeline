# Manifest Examples

This directory contains tiny illustrative examples of Script A outputs.

These files are:
- schema examples only,
- intentionally truncated,
- non-authoritative,
- and not provenance-preserving runtime artifacts.

Real BioProject acquisition provenance is generated dynamically by Script A and should remain local to the execution environment.