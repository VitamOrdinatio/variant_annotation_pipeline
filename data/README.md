# Data Directory

Large FASTQ, BAM, VCF, and full TSV outputs are excluded from Git.

This directory preserves the expected project structure:

- `raw/`
- `interim/`
- `processed/`

Representative outputs are tracked under `docs/examples/`.