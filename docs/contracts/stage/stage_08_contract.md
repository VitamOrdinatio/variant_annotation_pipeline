# Stage 08 — Filter & Partition Contract

**Pipeline:** Variant Annotation Pipeline (VAP)
**Stage:** 08
**Status:** Specification (Pre-Implementation)

---

# 🧭 Purpose

Stage 08 converts **raw VEP annotation output** into a **biologically structured, lossless, interpretation-ready dataset**.

It is the first stage that transitions VAP from:

```text
toolchain execution → biological interpretation system
```

---

# 🎯 Design Principles

1. **Lossless transformation**

   * No biologically plausible variants are permanently discarded
   * All original VEP data must remain recoverable

2. **Separation of concerns**

   * Stage 08 = classification + partitioning
   * Stage 09+ = interpretation + prioritization

3. **Transcript-aware design**

   * Do NOT collapse transcript-level consequences prematurely

4. **Deterministic output**

   * Same input → identical partition outputs

---

## Downstream Compatibility Requirements

Stage 08 outputs must preserve the minimum fields required for future VDB ingest and RDGP gene-level aggregation.

Required downstream-compatible fields:

- `sample_id`
- `run_id`
- `source_pipeline`
- `variant_id`
- `chromosome`
- `position`
- `reference_allele`
- `alternate_allele`
- `variant_type`
- `variant_class`
- `quality_flag`
- `gene_id`
- `gene_symbol`
- `transcript_id`
- `consequence`
- `impact_class`
- `clinical_significance`
- `clinvar_significance`
- `population_frequency`
- `gnomad_af`
- `exac_af`
- `thousand_genomes_af`
- `mito_flag`
- `epilepsy_flag`
- `annotation_source` (provenance field)
- `annotation_version` (provenance field)
- `gene_mapping_status`

Stage 08 must not rename these fields without documenting a schema mapping.

### Key rules to clarify fields

**1. consequence vs. impact_class** 

`consequence` holds fine-grained, categorical information about the variant pulled directly from vep, such as:
- missense_variant
- stop_gained
- splice_region_variant
- intron_variant

`impact_class` holds a coarse severity bin, such as:
- HIGH
- MODERATE
- LOW
- MODIFIER

Both must be preserved.


**2. Significance fields:**

`clinical_significance` must never overwrite or replace `clinvar_significance`.

  1. `clinvar_significance` = raw source truth (Pathogenic, Benign, etc.)
  2. `clinical_significance` = normalized interpretation field (aggregates from ClinVar and other sources)

Both must be preserved.


**3. population_frequency vs gnomad_af vs exac_af vs thousand_genomes_af fields:**

Aggregated:
- `population_frequency` holds a single summary value that is aggregated across source-specific AF fields

Source-specific AF fields (raw-evidence):
- `gnomad_af`
- `exac_af`
- `thousand_genomes_af`

All must be preserved.

`population_frequency` must be computed using a defined strategy:

Default `population_frequency` calculation for VAP v1:

```text
population_frequency = max(
  gnomad_af,
  exac_af,
  thousand_genomes_af
)
```

**Rationale:**
conservative estimate of population prevalence

If all source-specific AF fields are missing:

population_frequency = NULL
frequency_status = missing

---

## Gene Mapping Rule

If a variant lacks gene_id and gene_symbol:

- It must still be preserved in Stage 08 outputs
- It must be excluded from stage_08_rdgp_gene_evidence_seed.tsv
- It must be flagged:

gene_mapping_status ∈ {mapped, unmapped}


`gene_mapping_status` must be included in:
- stage_08_transcript_consequences.tsv
- stage_08_variant_summary.tsv

---

## Phenotype Flags (Optional)

Fields such as:
- mito_flag
- epilepsy_flag

must be:

1. deterministically derived
2. backed by a defined source (e.g., GSC gene set, ClinVar annotation)
3. documented in a separate phenotype_flag_spec.md

Stage 08 must not infer phenotype flags heuristically.

## Field Naming Compatibility

Stage 08 may use verbose field names (e.g., chromosome, position),
but must provide a documented mapping to interface-required fields:

chrom ↔ chromosome  
pos ↔ position  
ref ↔ reference_allele  
alt ↔ alternate_allele

All Stage 08 outputs intended for VDB ingest must either:
- conform to interface field names, OR
- include an explicit schema mapping layer.

---

## Null Handling

Stage 08 must distinguish:

- `NA` = unavailable from annotation source
- empty string = parsing failure or malformed input
- `0` = observed numeric zero

Stage 08 must not convert missing allele frequency to rare.
Stage 08 must not convert missing clinical significance to benign.

---


# 📥 Inputs

## Required

```text
annotated_variants.vcf
annotated_variants.tsv
```

Generated by Stage 07 (VEP)

---

## Required Fields (from VEP)

* CHROM
* POS
* REF
* ALT
* CSQ (VEP consequence field)
* SYMBOL
* Gene
* Feature (transcript)
* Consequence
* IMPACT
* BIOTYPE
* CANONICAL
* Existing_variation (if present)

---

# 🔄 Processing Logic

## Step 1 — Parse CSQ Field

* Expand VEP annotations into **one row per variant–transcript pair**
* Preserve ALL transcript consequences

Output:

```text
stage_08_transcript_consequences.tsv
```

---

## Step 2A — Assign Variant Context

`variant_context` describes the biological annotation context of the variant relative to genes, transcripts, or regulatory regions.

It must not be confused with `variant_type`, which describes allele structure.

variant_context ∈ {
  coding,
  splice_region,
  regulatory,
  intronic,
  intergenic,
  noncoding_transcript,
  unknown
}

---

## Step 2B — Normalize Variant Type

`variant_type` describes the allele structure, not the biological context.

variant_type ∈ {
  snv,
  insertion,
  deletion,
  indel,
  complex,
  unknown
}

If Stage 07 provides uppercase values such as `SNV`, Stage 08 must normalize case deterministically.

## Step 2C — Normalize Variant Class

`variant_class` represents a higher-level classification of variant function
(e.g., coding vs noncoding), distinct from both `variant_type` and `variant_context`.

variant_class ∈ {
  coding,
  noncoding,
  structural,
  unknown
}

### Relationships:

- `variant_type` = allele structure (SNV, indel, etc.)
- `variant_context` = biological location/effect context
- `variant_class` = coarse functional grouping used for downstream partitioning

If Stage 07 already provides `variant_class`, Stage 08 must normalize and preserve it.

---

## Example Classification Expectations

Stage 08 must correctly classify the following known Stage 07 examples:

### Example 1 — Noncoding transcript exon variant

Input:
- `variant_id`: `1:14522:G:A`
- `gene_symbol`: `WASH7P`
- `consequence`: `non_coding_transcript_exon_variant`
- `impact_class`: `MODIFIER`
- `variant_class`: `non-coding`

Expected:
- `variant_context`: `regulatory` or `noncoding_transcript`
- `variant_effect_severity`: `MODIFIER`
- `interpretability_status`: `needs_external_annotation`

### Example 2 — Missense variant

Input:
- `variant_id`: `1:953279:T:C`
- `gene_symbol`: `NOC2L`
- `consequence`: `missense_variant`
- `impact_class`: `MODERATE`
- `variant_class`: `coding`

Expected:
- `variant_context`: `coding`
- `variant_effect_severity`: `MODERATE`
- `interpretability_status`: `interpretable_now`

### Example 3 — Stop-gained variant

Input:
- `variant_id`: `1:48242556:G:T`
- `gene_symbol`: `SLC5A9`
- `consequence`: `stop_gained`
- `impact_class`: `HIGH`
- `variant_class`: `coding`

Expected:
- `variant_context`: `coding`
- `variant_effect_severity`: `HIGH`
- `interpretability_status`: `interpretable_now`


## Step 3 — Assign Effect Severity

Based on VEP `IMPACT` and `Consequence`:

```text
variant_effect_severity ∈ {
  HIGH,
  MODERATE,
  LOW,
  MODIFIER,
  UNKNOWN
}
```

Mapping must remain transparent and reproducible.

---

## Step 4 — Assign QC Status

```text
qc_status ∈ {
  pass,
  caution,
  fail
}
```

`quality_flag` must be preserved exactly as received from upstream stages,
in addition to derived `qc_status`.

### Criteria

* Missing essential fields → fail
* Ambiguous annotation → caution
* Otherwise → pass

QC status must be preserved in all outputs.

Downstream systems (RDGP) may:
- exclude
- penalize
- or report low-quality variants

Stage 08 must not remove them.


---

## Step 5 — Assign Derived Annotation Labels

Stage 08 may derive additional annotation labels from existing fields.
These labels must be deterministic, reproducible, and non-destructive.

### 5.1 Interpretability Status

interpretability_status ∈ {
  interpretable_now,
  needs_external_annotation,
  unsupported_currently
}

---

### 5.2 Frequency Status

Derived from:
- population_frequency
- gnomad_af
- exac_af
- thousand_genomes_af

frequency_status ∈ {
  common,
  low_frequency,
  rare,
  missing,
  unknown
}

#### Suggested thresholds (v1, configurable later):

- common: AF ≥ 0.05
- low_frequency: 0.01 ≤ AF < 0.05
- rare: AF < 0.01
- missing: no AF fields populated
- unknown: conflicting AF signals across sources

Stage 08 must preserve raw AF values alongside this label.

---

### 5.3 Clinical Status

Derived from:
- clinical_significance
- clinvar_significance

clinical_status ∈ {
  pathogenic,
  likely_pathogenic,
  vus,
  likely_benign,
  benign,
  conflicting,
  missing
}

#### Rules:

- conflicting: multiple annotation sources disagree
- missing: no clinical annotation available

Stage 08 must preserve raw clinical annotation fields.

---

## Step 6 — Variant-Level Aggregation

Collapse transcript-level rows into variant-level summary:

Output:

```text
stage_08_variant_summary.tsv
```

### Required fields

* variant_id (CHROM:POS:REF:ALT)
* gene_symbols (set)
* worst_consequence
* highest_impact
* canonical_present (boolean)
* coding_flag (boolean)
* splice_flag (boolean)
* noncoding_flag (boolean)
* transcript_count

---

## Step 7 — Partition Outputs

Partitioning must be based on `variant_context`, not `variant_class`.

`variant_class` is a higher-level grouping and must not override context-based partitioning.

Create the following files:

```text
coding_candidates.tsv
splice_region_candidates.tsv
noncoding_candidates.tsv
qc_flagged.tsv
```

### Rules

* A variant may appear in multiple partitions if applicable
* No variant is dropped unless irreparably malformed

---

# 📤 Outputs

## Required Files

```text
stage_08_transcript_consequences.tsv
stage_08_variant_summary.tsv
coding_candidates.tsv
splice_region_candidates.tsv
noncoding_candidates.tsv
qc_flagged.tsv
stage_08_summary.json
stage_08_vdb_ready_variants.tsv
stage_08_rdgp_gene_evidence_seed.tsv
```

`stage_08_vdb_ready_variants.tsv` is variant-centric and preserves one record per variant/transcript annotation suitable for VDB ingest.

`stage_08_rdgp_gene_evidence_seed.tsv` is not final RDGP scoring. It is a minimal `(sample_id, gene_symbol/gene_id)` aggregation seed containing variant counts, highest impact, and contributing variant IDs.

`stage_08_rdgp_gene_evidence_seed.tsv` must explicitly include the following fields:

- `sample_id`
- `gene_id`
- `gene_symbol`
- `variant_count`
- `high_impact_variant_count`
- `rare_variant_count`
- `pathogenic_variant_count`
- `max_variant_severity`
- `has_low_quality_evidence`
- `contributing_variant_ids`

---

## stage_08_summary.json

Must include:

* total_variants
* variants_by_context
* variants_by_severity
* qc_status_counts
* interpretability_counts
* frequency_status
* clinical_status
* variants_by_variant_type
* variants_by_variant_class

---

# 🔒 Invariants

1. **Losslessness**

```text
Every input variant must be present in at least one output table
```

2. **Transcript fidelity**

```text
No transcript-level information is discarded
```

3. **Determinism**

```text
Repeated runs produce identical outputs
```

4. **Schema stability**

```text
Column names and formats must remain consistent
```

5. **Sample identity preservation**

Every output row must retain `sample_id`.

This is required for downstream `(sample_id, gene_id)` aggregation in RDGP.

6. **Annotation multiplicity preservation**

If a variant has multiple annotation rows:
- all must be preserved
- no implicit precedence or collapsing occurs in Stage 08

---

# ⚠️ Explicit Non-Goals

Stage 08 MUST NOT:

* Perform pathogenicity scoring
* Apply population frequency filtering
* Rank variants
* Integrate external databases (ClinVar, gnomAD, etc.)
* Collapse variants to a single “best” transcript

---

# 🧠 Handoff to Stage 09

Stage 09 will consume:

```text
coding_candidates.tsv
stage_08_variant_summary.tsv
```

For:

```text
coding impact interpretation
```

---

# 🧪 Recommended Dev Mode

Use:

```text
1–100 variant VCF
```

To enable:

* rapid iteration
* schema validation
* debugging

---

# 🎯 Bottom Line

Stage 08 is the **critical boundary layer** that:

```text
translates VEP output → structured biological representation
```

It must remain:

* lossless
* deterministic
* interpretation-ready
* future-proof

---

# END

